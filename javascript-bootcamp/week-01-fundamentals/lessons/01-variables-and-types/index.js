let crypto = "ETH";
let walletLabel = "Trading Wallet";
let cryptoAmount = 3;
let cryptoPrice = 3800;
let isActive = false;

console.log(
  `${walletLabel}: ${cryptoAmount} ${crypto} at $${cryptoPrice.toLocaleString()} each. Active: ${isActive}.`
);
