# Week 1 Starter

Run this folder with:

```bash
node index.js
```

Files:

- `index.js` runs the exercises and calculator example.
- `exercises.js` contains small fundamentals challenges.
- `calculator.js` contains the investment-calculator functions.

Do not look for a package install step—this starter uses only Node.js.
