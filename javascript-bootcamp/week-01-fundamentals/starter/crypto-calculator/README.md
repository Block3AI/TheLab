# Crypto Calculator

This Node.js program calculates the value of a Bitcoin holding by multiplying a BTC amount by its price (manually input). It runs three sample cases and formats each result as currency.

## Run it

From this folder, run:

```bash
node index.js
```
